{
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "sharp": "^0.35.3",
    "typescript": "^7.0.2",
    "vite": "^8.2.1"
  },
  "scripts": {
    "compile": "tsc",
    "watch": "tsc --watch",
    "dev": "vite",
    "host": "vite --host",
    "generate-thumbs": "node scripts/generate-thumbs.ts",
    "build": "tsc && npm run generate-thumbs && vite build",
    "build:shop": "tsc && npm run generate-thumbs && VITE_SHOP=1 vite build",
    "dev:shop": "VITE_SHOP=1 vite",
    "preview": "vite preview"
  }
}
